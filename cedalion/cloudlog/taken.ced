'/bootstrap':signature(taken(Inc,Session)::'/impred':impred,[Inc::counterValue,Session::'/bootstrap':string]).
'/deploy':behavior(taken(Inc,Session)::'/impred':impred,!'should succeed when Session matches the one one in Inc','/impred':simulate(taken(inc(2,!'session1',[],1234),!'session1'),[],'/impred':true)).
'/impred':clause(taken(inc(_,Session,_,_),Session),'/impred':true).
'/deploy':behavior(taken(Inc,Session)::'/impred':impred,!'should succeed if the timestamp in Inc is smaller than the current time','/impred':simulate(taken(inc(2,!'session1',[],1000),!'session2'),['/impred':task('/impred':now,2000,'/bootstrap':number)],'/impred':true)).
'/impred':clause(taken(inc(_,_,_,IncTime),_),'/impred':conj('/impred':task('/impred':now,Now,'/bootstrap':number),'/impred':pred(Now>IncTime))).
'/bootstrap':sandbox('/impred':simulate(taken(inc(2,!'session1',[],1000),!'session2'),['/impred':task('/impred':now,2000,'/bootstrap':number)],'/impred':true)::'/bootstrap':pred).
